<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="116dp"
    android:background="@color/gray_200">

    <ImageView
        android:id="@+id/user_album_cover_image"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_marginRight="16dp"
        android:scaleType="centerCrop"
        android:src="@mipmap/default_buddyicon"/>

    <TextView
        android:id="@+id/user_album_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:layout_toRightOf="@id/user_album_cover_image"
        android:text="Flickr API"
        android:typeface="sans"
        android:textSize="16sp"
        android:textColor="@color/black"/>

    <TextView
        android:id="@+id/user_album_brief"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="24dp"
        android:layout_toRightOf="@id/user_album_cover_image"
        android:typeface="sans"
        android:textColor="@color/black"
        android:text="936 Photos"/>

    <View
        android:layout_height="16dp"
        android:layout_width="match_parent"
        android:layout_alignParentBottom="true"
        android:layout_below="@id/user_album_cover_image"
        android:background="@android:color/background_light"/>

</RelativeLayout>